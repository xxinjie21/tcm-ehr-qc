<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tcm.ehr.mapper.RecordMapper">

    <resultMap id="BaseResultMap" type="com.tcm.ehr.entity.Record">
        <id column="id" property="id"/>
        <result column="registration_no" property="registrationNo"/>
        <result column="outpatient_no" property="outpatientNo"/>
        <result column="gender" property="gender"/>
        <result column="age" property="age"/>
        <result column="visit_count" property="visitCount"/>
        <result column="western_diagnosis" property="westernDiagnosis"/>
        <result column="tcm_diagnosis" property="tcmDiagnosis"/>
        <result column="present_illness" property="presentIllness"/>
        <result column="chief_complaint" property="chiefComplaint"/>
        <result column="self_report" property="selfReport"/>
        <result column="inspection" property="inspection"/>
        <result column="pulse" property="pulse"/>
        <result column="tongue" property="tongue"/>
        <result column="physical_exam" property="physicalExam"/>
        <result column="pattern" property="pattern"/>
        <result column="prescription" property="prescription"/>
        <result column="follow_up" property="followUp"/>
        <result column="treatment_effect" property="treatmentEffect"/>
        <result column="department" property="department"/>
        <result column="doctor_id" property="doctorId"/>
        <result column="visit_time" property="visitTime"/>
        <result column="structured_data" property="structuredData"/>
        <result column="qc_results" property="qcResults"/>
        <result column="score" property="score"/>
        <result column="grade" property="grade"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="allColumns">
        id, registration_no, outpatient_no, gender, age, visit_count,
        western_diagnosis, tcm_diagnosis, present_illness, chief_complaint,
            self_report, inspection, pulse, tongue, physical_exam, pattern,
        prescription, follow_up, treatment_effect, department, doctor_id,
        visit_time, structured_data, qc_results, score, grade, status
    </sql>

    <insert id="insert" parameterType="com.tcm.ehr.entity.Record">
        INSERT INTO records (
            id, registration_no, outpatient_no, gender, age, visit_count,
            western_diagnosis, tcm_diagnosis, present_illness, chief_complaint,
        self_report, inspection, pulse, tongue, physical_exam, pattern,
            prescription, follow_up, treatment_effect, department, doctor_id,
            visit_time, structured_data, qc_results, score, grade, status
        ) VALUES (
            #{id}, #{registrationNo}, #{outpatientNo}, #{gender}, #{age}, #{visitCount},
            #{westernDiagnosis}, #{tcmDiagnosis}, #{presentIllness}, #{chiefComplaint},
            #{selfReport}, #{inspection}, #{pulse}, #{tongue}, #{physicalExam}, #{pattern},
            #{prescription}, #{followUp}, #{treatmentEffect}, #{department}, #{doctorId},
            #{visitTime}, #{structuredData}, #{qcResults}, #{score}, #{grade}, #{status}
        )
    </insert>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM records WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM records
    </select>

    <select id="findByIds" resultMap="BaseResultMap">
        SELECT * FROM records WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
    </select>

    <update id="updateCleanFields">
        UPDATE records
        SET gender = #{gender}, age = #{age}, pattern = #{pattern},
            prescription = #{prescription}, status = #{status}, grade = #{grade}
        WHERE id = #{id}
    </update>

    <select id="selectOverview" resultType="map">
        SELECT
            COUNT(*) AS totalRecords,
            COALESCE(SUM(CASE WHEN grade = '合格' THEN 1 ELSE 0 END), 0) AS qualifiedCount,
            COALESCE(SUM(CASE WHEN status = 'pending' THEN 1 ELSE 0 END), 0) AS pendingReviewCount,
            COALESCE(SUM(CASE WHEN status = 'invalid' THEN 1 ELSE 0 END), 0) AS invalidCount
        FROM records
    </select>

    <select id="selectGovernanceStats" resultType="map">
        SELECT
            COALESCE(SUM(CASE WHEN grade = '合格' THEN 1 ELSE 0 END), 0) AS qualified,
            COALESCE(SUM(CASE WHEN grade = '合格' AND governed = 1 THEN 1 ELSE 0 END), 0) AS governedCount,
            COALESCE(SUM(CASE WHEN grade = '合格' AND governed = 0 THEN 1 ELSE 0 END), 0) AS pendingGovern
        FROM records
    </select>

    <update id="markGoverned">
        UPDATE records SET governed = 1 WHERE id = #{id}
    </update>

    <update id="updateStructuredData">
        UPDATE records SET structured_data = #{structuredData} WHERE id = #{id}
    </update>

</mapper>
