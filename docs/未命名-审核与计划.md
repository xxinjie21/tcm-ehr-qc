# 未命名.md 审核结论

> 来源：`docs/未命名.md`（用户逐页反馈散记，31 行）。审核日期：2026-09-23。
> 结论分四类：**做**（进实施）、**已修**（无需改）、**暂不做**（进 backlog）、**忽略**（用户标注）。
> 实施批次见 `docs/待办计划.md`。

## 目录
- [一、逐条结论](#一逐条结论)
- [二、backlog](#二backlog)
- [三、已定决策](#三已定决策)

---

## 一、逐条结论

### 结构化解析
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | 批量解析上限需按数据集上限调整 | **做** | `NlpExtract.vue:289` 上限硬编码 `:max="500"`；批量是**前端串行循环**（`:629-665`），注释自认"后端暂无批量接口"（`:620-622`）。35355 条串行不可用、关页面即断 | 成员B线 | 批K·K-a |

### 质控检验
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | "规则引擎未报冲突"是什么意思 | **已修** | `QcServiceImpl:361-362` 已区分"判过确实无冲突"与"无规则可判"；`Qc.vue:41-44` 两种文案均在下发 | — | 无需改 |

### 清洗与导出
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | 归一后由 LLM 判定非精确命中实体的原文与标准名是否一致 | **暂不做**（用户 2026-09-23 定） | 归一自 2026-09-23 起以 **ES 为唯一权威**（`EsTermNormalizer`）；LLM 入自动归一链路会破坏确定性、不可复现、可能改错标准词。若将来要做，应做成"候选审阅 + 人工确认"而非自动回写 | 成员C线 | backlog |

### 术语字典
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | 版本回滚可读性要高，别让用户不知是什么时候的版本 | **做** | `Dictionary.vue:163-178` 仅显示文件名（`*.json.bak_yyyyMMdd_HHmmss`）+「回滚到此版」。补：可读时间 / 词条数 / 相对当前 ±N | 成员C线 | 批H·H1 |

### 日志审计
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | 加清理功能，防止日志太多 | **做** | `LogController` 现无删除能力（仅 list / actions / export） | 成员A线 | 批I·I2 |
| 2 | 操作日志接入 AI 助手，让助手知道用户操作了什么 | **做（轻量）** | `AiServiceImpl.buildContext:287-` 现仅取病历/统计，不含操作日志。方案：只注入**本人最近 10 条**、脱敏、不含 IP；LLM 关时走规则回答 | 成员B线 | 批I·I3 |

### 全局新页面约束
| # | 原话要点 | 结论 | 依据 / 现状 | 处理器 | 去向 |
|---|---|---|---|---|---|
| 1 | 下拉滑动才能填写，体验差 | **忽略**（用户标注） | — | — | — |
| 2 | 弹窗需下拉才能填写，建议左右分布 | **忽略**（用户标注） | — | — | — |
| 3 | 数据集里没有方剂，是否需要方剂词典 | **保留词典** | 源数据 `docs/电子病历精简脱敏数据_500行.xlsx` 21 列确无「方剂」（仅「草药」）；但 `formulas.json` 仍被 归一类型（`TermTypes`）、图谱（`Qc.vue:205`）、结构化（`NlpExtract` formulaList）、复核（`Review.vue:245`）消费。源数据缺列只意味着 NLP 抽不到，不构成删词典理由 | — | 无需改 |
| 4 | 每次打开都显示未启用 LLM、配置为空 | **做** | 根因：`LlmConfigStore` 只存内存（`LlmConfigStore.java:12-13`），重启回 `application.yml` 基线（`enabled:false` / `model:""` / `baseUrl:""`），故每次打开都空。方案：非密钥字段落盘 `data/llm-config.json`（gitignore），API Key **不落盘**、每次重填，弹窗回显已存通道/模型 | 成员C线 | 批I·I1 |
| 5 | 有 35355 份真实数据集，项目上限需调整 | **做** | 卡点散落：`application.yml:24` multipart 50MB、批任务条数上限、图谱 `MAX_SCAN_RECORDS`（`QcServiceImpl:280`）、导入后串行抽取（`RecordServiceImpl:402-424`）、性能口径（需求 3 万条） | 全组 | 批K·全局5 |
| 6 | 被结构化解析后的病历应显示参照哪版术语词典 | **做** | `structured_data` 现无版本字段。方案：写入结构化数据时打 `_meta={dictVersion,dictCapturedAt}`（JSON 保留键）。已核实 `StatsServiceImpl` / `QcScorer` 均按**指定 key** 取值、不遍历全 key，`_meta` 无污染 | 成员B线 | 批H·H2 |

---

## 二、backlog

| 项 | 来源 | 暂缓理由 |
|---|---|---|
| 归一后 LLM 判定 | 清洗与导出·1 | 破坏 ES 唯一权威口径；如需，做候选审阅 + 人工确认 |
| 解析真实"续跑" | 结构化解析·1 延伸 | 只做"中断标记 + 可重跑"（批K·K-c），不做断点续跑 |

---

## 三、已定决策

| 决策 | 取值 | 备注 |
|---|---|---|
| H2 词典版本存储 | **JSON 保留键 `_meta`** | 不改表；已核实无副作用 |
| 导入后自动抽取 | **去掉**（K0） | 导入只入库，解析统一在结构化解析页发起 |
| 批任务接口权限 | **仅管理员** | 与保存 `PUT /api/records/{id}`（仅管理员）一致 |
| 批任务重启处置 | `RUNNING` / `QUEUED` → **INTERRUPTED** | 不做重新入队，最省维护 |
| 批任务失败清单 | **只存前 500 条 + 截断标记** | 防 JSON 列被 35355 条撑爆 |
| 回滚备份"来源"列 | **不做** | 备份文件名不含来源信息 |
