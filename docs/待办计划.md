# 待办计划

> 只列**未完成**的项。已完成批次 **H / I / K / L** 见 `docs/未命名-审核与计划.md` 与 git 提交记录，此处不再重复。

## backlog
- **清洗1**：归一后由 LLM 判定非精确命中实体（候选审阅 + 人工确认）——**暂不做**。
  - 理由：归一现以 ES 为唯一权威，LLM 入自动链路会破坏确定性；若做，应做成"候选审阅 + 人工确认"。

---

## 已完成（供参考，不再跟踪）
- **批H**：术语词典回滚可读（时间/词条数/±N）；词典版本可追溯（`structured_data._meta`）。
- **批I**：LLM 非密钥配置持久化（Key 不落盘）；日志归档后清理（`logs/audit-archive-*.csv`）；日志进 AI 助手。
- **批K**：去掉导入自动抽取（K0）；批量解析改后端异步任务（`nlp_task` 表 + 队列 + 2 worker + 4 接口 + 前端轮询）；重启未完成任务标 `INTERRUPTED`；35355 上限核对；openapi/文档回写。
- **批L**：
  - L1 首页趋势图图例去重。
  - L2 LLM 模型改常用下拉（可选可自定义）。
  - L3 导入后自动解析改为用户开关（默认关，投后台任务）。
  - L4 AI 助手可拖动 + 多轮历史追问 + 日志 50 条 + 可查范围提示。
  - L5 质控检验图谱改为「冲突清单 + 规则命中对照 + 覆盖说明」表格。
  - L6 文案：质控"未报冲突"压缩；结构化保存按钮改「写回结构化数据」。
  - L7 批量解析上限改「全部（默认）/ 指定前 N 条」。
  - L8 说明：导入 / 批量解析 / 批量重算等**批量操作各只记 1 条汇总审计**（现状，非缺陷）。
- **批M**：质控评分口径重构——核心要素改 **5 项**（症状/证候/舌象/脉象/中药），判定以结构化结果为准，**两档扣分**（原始列有=漏抽 -6，两处皆无=真缺失 -12）；**治法、方剂退出评分**（数据源无该两列）；未解析 → `structuredMissing`、落待复核。逻辑/格式/分级线不变。
- **批N**：术语词典页前端修复——**去掉整页 zoom 一页适配**，改为与其它页一致的正常大小（表格内部滚动）；默认页签改「疾病」；移除词典页专属的 `pad-b-sm` 底部收紧。
- **批O**：病历删除增强——修外键（删病历前先清 `review_tasks`，否则待复核病历删不掉）；新增 **`POST /api/records/delete-by-filter`** 按筛选范围批量删除（条件全空拒绝，防误删全库）；前端「病历查询」页（原「病历代查」改名）保留勾选删除并新增「删除范围内病历」。
- **批P**：质控标准可视化——`GET /api/qc/rules`（登录即可）只读下发评分口径与逻辑规则；`GET /api/qc/deduction-stats`（管理员）范围扣分聚合；前端质控校验页新增可折叠「质控评分标准」「本范围扣分构成」面板、扣分明细弹窗内加「评分构成瀑布」；**原「规则证据」三表已按用户要求移除**。
- **批Q**：质控规则可配置（通用、换数据集可用）——规则集 `QcRuleSet` + `QcRuleStore`（`data/qc-rules.json`，缺失用内置默认、字段级深合并、关键项非法回默认告警）；`QcScorer`/`LogicChecker` 改为按规则执行，规则依赖要素缺失时自动"不适用"；接口 `GET/PUT /api/qc/rules` + `POST /api/qc/rules/reset`（PUT/reset 管理员）；前端「规则配置」弹窗（全量编辑：完整性/格式/一致性/标准化/阈值）+ 标准面板按配置渲染。内置默认按当前数据集 10 证型配对调优。移除无用 `GET /api/qc/graph`（含 GraphVO）。接口数 50。
- **批R**：规则配置"说人话"——后端 `GET /api/qc/rules` 增 `descriptions`（自然语言，与规则同源）+ `catalogElements`/`catalogFormats` 目录；前端标准面板直接展示句子，配置弹窗改为「句子清单 + 就地编辑」（中文下拉、开关、数字，不出现 key/正则/逗号表），保留「恢复默认」一键还原；不做高级 JSON 模式。
- **批S（进行中）**：实体类型统一目录——固定 9 类 `EntityTypes` 作为 结构化解析/术语词典/质控规则 的单一来源（词典类型=其中 5 类，**不做用户自定义**）。
  - **S1（已完成）**：新增 `EntityTypes`；`TermTypes`/`EntityNormalizer.dictionaryType`/`DictionaryFileServiceImpl.fileNameOf`/`GovernanceServiceImpl`/`GovernanceController`/`QcRuleDescriber.catalogElements` 改读目录。零判定行为变更；接口 50。
  - **S2（已完成）**：质控对接目录——完整性要素下拉=9 类（默认 6：症状/疾病/证候/舌象/脉象/中药）；标准化=5 词典类；一致性泛化为 `{触发类型,期望类型,触发值[],期望值[]}` 且期望值仅从词典选；默认一致性=证候→中药 9 条 + 证候→方剂 5 条（词典值）；旧版一致性条目加载时忽略并告警。
  - **S3（待做）**：前端 `structured.js`/`Dictionary.vue`/`Qc.vue` 改读 `/api/entity-types` + 词典；文档口径同步。

---

## 决策记录
| 项 | 结论 |
|---|---|
| `未命名.md` 里的 `##` 标记 | 误复制，无含义 |
| 批量任务条数上限 | 「全部（默认）/ 指定前 N 条」 |
| 批量操作日志 | 各 1 条汇总 |
| 清洗1（归一 LLM） | backlog |
| 质控图谱 | 只留表格，弃力导向关系图 |
| 方剂词典 | 保留（源数据无方剂列，词典仍服务归一/图谱/逻辑） |

## 横向约定
- DB 改动只进 `database-init.sql`；现网库结构变更由用户手动执行。
- 加接口须同步 `docs/中医电子病历质控与标准化系统-openapi.yaml`。
- 临时验证端口 8081（用户经 IntelliJ 起 8080）。
