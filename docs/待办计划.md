# 待办计划

> 只列**未完成 / 没做好**的项。已完成批次 **H / I / K** 及结论见 `docs/未命名-审核与计划.md`，此处不再重复。
> 状态：**待执行**。每项流程：改代码 → 构建/冒烟 → 同步文档 → `git add` 指定文件 → 提交（**push 依赖 Steam++**）。

## 目录
- [批L](#批l未完成项)
- [backlog](#backlog)
- [决策记录](#决策记录)
- [顺序与验收](#顺序与验收)
- [横向约定](#横向约定)

---

## 批L（未完成项）

### L1 首页·趋势图图例重复（低·前端）
`Dashboard.vue` 趋势图图例 `合格率 / 待复核数` 与右侧坐标轴名 `待复核` 重复，看着像两个"待复核"。
- 改：图例统一为 `合格率 / 待复核`，去掉右轴 `name`，单位放 tooltip。
- 验收：图例只有一个"待复核"项。

### L2 LLM·模型下拉（低·前端）
`LlmConfigDialog.vue` 模型名输入框改 `el-select allow-create filterable`：
- ollama：`qwen2.5:7b / qwen2.5:14b / llama3.1 / deepseek-r1:7b`
- openai 兼容：`deepseek-chat / deepseek-reasoner / gpt-4o-mini / qwen-plus / glm-4 / moonshot-v1-8k`
- 允许手输自定义；提示由"留空用通道默认模型"改为"从下拉选择或输入自定义模型名"（openai 留空会退到 SDK 默认 `gpt-4o-mini`，对三方无效，故不再宣称留空可用）。

### L3 导入·自动解析开关（中·前后端）
- 前端 `Records.vue` 导入页加开关「导入后自动结构化解析」，**默认关**；说明"开启后导入秒回，解析交给后台队列"。
- 后端 `POST /api/records/import` 加参数 `autoExtract`（默认 false）；为真且 `nlp.enabled=true` 时，导入完成投 **NlpBatch 任务**（复用批K队列，非同步串行）。
- `NlpBatchService` 加"按记录ID集合"提交：任务 `filters_json` 存 `{"recordIds":[...]}`，`runTask` 见 `recordIds` 用 `in("id", ids)` 取数（管理员域）。
- `nlp.enabled=false`：不投任务，导入结果提示"未开启抽取，已跳过自动解析"。
- openapi：import 接口补参数说明（**接口数不变**）。

### L4 AI 助手·完善（中·前后端）
- **可拖动**：悬浮球 + 面板支持拖动，位置存 `localStorage`。
- **历史 + 追问**：面板改多轮对话（保留本条会话 Q/A 列表），追问带上文；后端 `AiQueryDTO` 加 `history` 注入 prompt，规则兜底同步。
- **日志范围提示**：面板明示"可查范围：本人最近 50 条操作 · 看板统计 · 当前病历"。
- **日志加量**：`AiServiceImpl` 本人操作日志 `10 → 50`。
- 无新增接口（`/api/ai/chat` 复用，DTO 加字段）。

### L5 质控图谱·换表示（中·前端）
弃力导向图，改三块表格（数据仍用 `/api/qc/graph`，不新增接口）：
1. **冲突清单**：每行 = 冲突原因 + 涉及证候/实体；无冲突写"未发现冲突"。
2. **规则命中对照**：每行 = 证候 → 合法治法 / 合法方剂。
3. **覆盖说明**：出现但规则表未覆盖的证候单列"未覆盖，不判冲突"；`coveredPatterns` 为空时整体提示"本次无可判规则"。
- 顶部汇总：范围病历数 / 覆盖证候数 / 规则命中数 / 冲突数。
- 只留表格，不保留关系图。

### L6 文案修正（低·前端）
- 质控"逻辑冲突·规则引擎未报冲突…"压缩为一句："规则表只覆盖少数证候，未覆盖的不判冲突；空白 ≠ 已核对"。
- 结构化解析"保存到病历"按钮改「**写回该病历的结构化数据**」+ tooltip（覆盖该条 `structured_data`，非新增病历）。

### L7 批量解析上限·改模式（低·前后端）
现状数字框默认 50、上限 100000，默认偏小。改为模式选择：
- 单选「处理范围」：**全部（默认）** / **指定前 N 条**；
- 选"指定"才出数字框，默认 `1000`、上限 `100000`；
- 提交后按后端返回 `total` 显示"本次处理约 X 条"；提示"条数多时后台耗时较长"。
- 后端不改（`limit` 空 = 全部已支持）。

### L8 文档说明（低·文档）
写明：导入 / 批量解析 / 批量重算等**批量操作各只记 1 条汇总审计**（现状即如此，非缺陷）。

---

## backlog
- **清洗1**：归一后由 LLM 判定非精确命中实体（候选审阅 + 人工确认）——**暂不做**。

---

## 决策记录
| 项 | 结论 |
|---|---|
| `未命名.md` 里的 `##` 标记 | 误复制，无含义 |
| 批量任务条数上限 | 用"全部（默认）/ 指定前 N 条" |
| 批量操作日志 | 保持各 1 条汇总 |
| 清洗1（归一 LLM） | 继续 backlog |
| 质控图谱 | 只留表格，弃力导向关系图 |
| 方剂词典 | 保留（源数据无方剂列，词典仍服务归一/图谱/逻辑） |

---

## 顺序与验收
顺序：**L1 → L2 → L6 → L7 → L3 → L4 → L5**。
每项：`npm run build`（前端）+ `mvn -o compile`（后端，`JAVA_HOME=F:\jdk24`）+ 冒烟（临时 8081，需要时开 :8001/ES）+ 文档同步（接口数仍 46，仅 import 参数变化）→ 提交 → push（Steam++）。

## 横向约定
- DB 改动只进 `database-init.sql`；现网库结构变更由用户手动执行。
- 加接口须同步 `docs/中医电子病历质控与标准化系统-openapi.yaml`。
- 临时验证端口 8081（用户经 IntelliJ 起 8080）。
