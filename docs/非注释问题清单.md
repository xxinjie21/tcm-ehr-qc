# 注释改造过程中发现的非注释问题清单

> 来源：2026-09-26 全库注释审核与补齐（前后端 + Python）。
> 初版只记录、不做修改。
>
> **2026-09-26 复核与处置**：逐条核实全部 14 项，并对全仓库做同类扫描（死代码 / 注释不符 / 7 类疑似缺陷）。
> 结果：**12 项成立、1 项不成立（C7）、1 项已修复（D14）**；另新发现同源问题若干。
> 已修复项见文末「已处理」，仍未处理项见「待办」。

---

## 一、原清单逐条核实

### A 死代码 —— 3 项全部成立，已清理

| # | 条目 | 结论 | 位置 |
|---|---|---|---|
| A1 | `QcScorer.strList` 私有静态、零调用 | **成立** | `common/utils/QcScorer.java` |
| A2 | `QcServiceImpl.pick` 私有、零调用 | **成立** | `service/impl/QcServiceImpl.java` |
| A3 | `StatsServiceImpl.str` 私有、零调用 | **成立** | `service/impl/StatsServiceImpl.java` |

> 注：同名 `str` 在 `AiServiceImpl` / `GovernanceServiceImpl` / `EsTermIndexServiceImpl` 均在用，未动。

### B 注释与实现不符 —— 3 项全部成立，已修正

| # | 条目 | 结论 | 说明 |
|---|---|---|---|
| B4 | `Records.vue` FIELD_GROUPS JSDoc 说「5 组、第一组默认展开」，实现是常显 | **成立** | JSDoc 停留在旧折叠方案，已改为与实现一致 |
| B5 | `StatsServiceImpl` 类注释「500 条内内存聚合」，实际全量载入 | **成立** | 已改为实情（无条数上限） |
| B6 | `NlpBatchServiceImpl.cancel()` 对 RUNNING 任务返回状态仍是 `RUNNING` | **成立** | 行为属实（只置取消位，worker 收尾）；**未改**，见待办 |

### C 疑似缺陷 —— 6 项成立、1 项不成立

| # | 条目 | 结论 | 位置 |
|---|---|---|---|
| C7 | `Review.vue` 一处三元两分支文案相同 | **不成立** | 已列全 Review.vue 11 处三元逐条比对：`L176` 实为 `剩余问题：` / `剩余问题：无`，**文案不同**；全前端正则扫「两分支字面量一致」= 0 命中。**从清单移除** |
| C8 | `NlpExtract.vue` `text` ref 只写不读 | **成立** | 已清理 |
| C9 | `Governance.vue` `scopeText` 在 `filters` 声明前引用（TDZ） | **成立** | 见待办 |
| C10 | `DictionaryFileServiceImpl` 备份名秒级时间戳会覆盖 | **成立** | 已修复 |
| C11 | `QcServiceImpl.BATCH_LOCK_KEY` 全局单锁 | **成立** | 见待办 |
| C12 | `DictionaryServiceImpl.searchTerms` 静默截断 100 条无提示 | **成立** | 前端显示「共 100 条」，实为上限；见待办 |
| C13 | `AiAssistant.vue` `ask()` 同一问题传两遍 | **成立** | 见待办 |

### D 口径并存 —— 已修复，从清单移除

| # | 条目 | 结论 |
|---|---|---|
| D14 | 「核心字段缺失」质控侧 6 要素 vs AI 侧 5 项 | **已修复（不存在）**。`AiServiceImpl.coreMissing` 现委托 `QcScorer.missingElements(data, r, qcRuleStore.get())`，与质控侧**同源**；Javadoc 明写「两侧共用同一份要素清单与判空口径」。**从清单移除** |

---

## 二、同类扫描新发现

### A 类 · 其他死代码 —— 12 处，已清理

Java：`EntityTypes.dictTypes()`（与 `dictKeys()` 重复）、整个 `domain/dto/UpdateRecordDTO.java`（已被裸 `Map` 取代）、6 个未用 import（`LlmClient`×2、`DictionaryController`、`GovernanceController`、`ReviewController`、`DictionaryServiceImpl`）。

前端：`AuditLog.vue` 未用 `ElMessageBox`、`Qc.vue` `standardOpen`（模板无绑定）、`Governance.vue` `closeDetail`（未绑事件）、`api/records.js` `importStatus`、`api/stats.js` `getStats`（均零调用）。

> Python（`python-nlp`）：0 处。Java `private` 字段：0 处（55 个 count==1 全是 Lombok `@Data` 的 DTO 字段）。

### B 类 · 其他注释与实现不符 —— 13 处，已修正

- **同一文件内自相矛盾**：`EntityNormalizer` JSDoc「7 类」vs 行内「8 类」；`Records.vue` 模板注释「12 行」vs CSS 注释「11 行」。
- **数量类**：`DeductionStatsVO` 枚举 4 维 vs 实际 5 维（漏「术语未标准化」）；`AiServiceImpl` 知识常量「三层扣分」vs 五类；`NlpExtract.vue`「21 个实体」vs 9 类分区。
- **行为类**：`StatsServiceImpl` 中药取值/回退说明与实现不符；`Governance` 第 3 步对用户称「格式规整」vs 实现「空值规整」（后端 2 处 + 前端 2 处）。
- **废弃方案残留**：`DeductionStatsVO`「沿用质控图谱口径」（模块已删）；`NlpExtract.vue`「走 ES 还是内存」（能力已撤）；`StructuredDataCard.vue`「走哪条路」（标签已无途径）；`DictionaryServiceImpl`「子控默认已开」（代码兜底 false，靠 yml）。

### C 类 · 其他疑似缺陷（清单外）

**🔴 高危（已修复）**

| 位置 | 问题 |
|---|---|
| `Review.vue` `FIELD_BY_ITEM` | **真 bug**：注释写「核心 6 要素」，映射表只有 5 项**缺「疾病」**；`estimate` 取到 `undefined` 便跳过加回 → 疾病缺失时预估评分**系统性偏低**。已补 `疾病: 'diseases'`（`COMPARE_FIELDS` 本就有该键，链路完整） |
| `Records.vue` 表单校验 | `max: 64` vs `database-init.sql` 的 `VARCHAR(50)`，后端无长度校验 → 51~64 字经 API 直调会撞 MySQL 严格模式。已改为 50 |
| `LogServiceImpl` 归档 | 同类于 C10：秒级名 + `Files.write` 默认截断 → 同一秒两次清理归档互相覆盖，**已删库数据无法追回**；且注释「不会互相覆盖」是错的。已加毫秒 + 去重序号 |
| `ReviewDTO.comment` | 前端 `Review.vue` 有输入框并发送，**后端全仓无 `getComment()`**、库表亦无列 → 复核意见 100% 丢失。已并入操作日志 `detail`（`TEXT` 列） |
| `python-nlp` `truncated` | Python 算出并回传，前端只引用了 `failureTruncated`/`dedStats.truncated`，**单条抽取的截断从未展示** → 尾部实体静默丢失。见待办 |

**🟡 中低危（见待办）**

- **静默截断无提示**（6 处）：任务列表 `LIMIT 50`、扣分明细前 20、趋势 12 个月、复核列表跳过已删病历、失败样本前 50、批次失败清单。
- **只写不读**（6 处）：`LogicCheckDTO.herbList`（收了不用 → `expectType=herb` 规则无法校验）、`ScoreResultVO.serious/checkedAt`、`RecordServiceImpl.taskStore`（导入状态无人查，`/records/import/{taskId}/status` 零消费者）。
- **TDZ 隐患**（8 处，全在前端）：`Dictionary.vue` watch 回调引用 6 个后置声明、`:333`/`:344` 相互引用后置函数、`Qc.vue`、`Records.vue` 等。Java 侧初始化顺序无问题。
- **全局锁/单键**（14 处）：`NlpBatchServiceImpl` 单队列 2 线程（一人提任务全员排队）、`LOCK_TTL_SECONDS=900` 固定（3 万条可能超时导致并发双跑）、`LlmClient` 重建锁、`OperationLogger` 实例锁、4 处 static 可变 map。
- **秒级时间戳**（另 3 处）：`GovernanceServiceImpl.ts()` 导出名、前端 `AuditLog.vue`/`Governance.vue` 导出名。已加毫秒/序号。
- **同一数据传两遍**（4 处）：`Review.vue` 列表已带全文又单独回查、`ReviewServiceImpl` 每页内联整份 `structuredData`。
- **前端校验与 DB/后端不一致**（5 处）：就诊次数前端 ≥1 后端无 `@Min`、密码前端 ≥6 位后端无 `@Size`、科室/医生前端无长度限制 DB 50 字、性别枚举三套口径。

---

## 三、已处理（2026-09-26）

| commit | 内容 |
|---|---|
| `08b15b7` | 修复复核预估评分漏算疾病、登记号/门诊号校验超列宽（64→50）、复核意见丢失（并入操作日志）、文件名秒级覆盖（备份/归档/导出 5 处） |
| `ff70176` | 修正 13 处与实现不符的注释（要素数、扣分维度、流水线步骤名、废弃残留等） |
| `cea20ab` | 清理 15 处死代码（3 个 Java 方法 + `dictTypes()` + `UpdateRecordDTO` 类 + 6 个 Java import + 5 处前端符号） |

**从清单移除**：C7（不成立）、D14（已修复）。

---

## 四、待办（未处理，按建议优先级）

| 优先级 | 项 | 说明 |
|---|---|---|
| 高 | `python-nlp` 截断无提示 | 单条抽取尾部实体静默丢失，`NlpExtractVO.truncated` 前端无展示 |
| 高 | C12 词典 100 条截断无提示 | 接口加 `total` 或前端标注「仅显示前 100 条」 |
| 中 | C13 AiAssistant 同句传两遍 | `ask()` 先 `push` 再 `historyText()` 读回，模型重复加权 |
| 中 | C9 / TDZ 8 处 | `Governance.vue` 及 `Dictionary.vue` / `Qc.vue` / `Records.vue`；把被引用声明前移即可根除 |
| 中 | C11 全局单锁 + `LOCK_TTL_SECONDS` 固定 | 多用户批量重算串行；TTL 可能短于大批次耗时 |
| 中 | 只写不读字段 | `LogicCheckDTO.herbList`（功能缺失）、`ScoreResultVO.serious/checkedAt`、`RecordServiceImpl.taskStore` + 其接口 |
| 中 | 静默截断其余 5 处 | 扣分明细前 20、趋势 12 个月、复核跳过已删病历、失败样本前 50、任务列表 50 |
| 中 | 前端校验与后端/DB 不一致 5 处 | 就诊次数 / 密码 / 科室长度 / 性别枚举 |
| 低 | 同一数据传两遍 4 处 | `Review.vue`、`ReviewServiceImpl` |
| 低 | static 可变 map、`LlmClient` 重建锁、`OperationLogger` 实例锁 | 加固项 |

> B6（`cancel()` 返回 `RUNNING`）保留原状：属**展示语义**问题，不涉及数据错乱；若前端需要「取消中」独立态，再决定是否新增状态值。
