# 中医电子病历质控与标准化系统 - 后续开发任务单

> 版本：v1.0 · 2026-09-17
> 工作流：每阶段 **前端先行（可 mock 数据）→ 提交审核 → 后端落地 → 验证提交**。
> 前置：后端已按 insurance 分层重构，MyBatis-Plus、JWT 鉴权、GlobalExceptionHandler 就绪；测试数据 500 条（seed-database.py）。

---

## 0. 开发基线

| 项 | 值 |
|----|----|
| 后端 | Spring Boot 3.5.4 · JDK17 · Maven · 端口 **8080**（手动启动） |
| 前端 | Vue3 + Vite6 + Element Plus + Pinia + ECharts 5.6 · **localhost:3000** · Vite 代理 `/api → 8080` |
| 中间件 | Docker Compose `D:\docker\windows`（MySQL / Redis / ES 7.17+IK / RabbitMQ / Nacos 等 13 个） |
| 账号 | `admin/123456`（管理员）· `auditor/123456`（审核员） |
| 测试数据 | `data/seed-database.py` 灌 500 条；词典 `data/dictionaries/{diseases,patterns,symptoms,herbs,formulas}.json` |
| ES 索引 | `term_disease` / `term_pattern` / `term_symptom` / `term_herb` / `term_formula`（IK 7.17.22） |
| 契约源 | `docs/中医电子病历质控与标准化系统-openapi.yaml`（**唯一接口契约基准**） |
| 配置注意 | `application.yml`：`dictionary.dir` 绝对路径（换机需改）、`elasticsearch.score-threshold=0.8`、`nlp.enabled` |

---

## 1. 任务范围与依赖

### 1.1 范围

| 线 | 内容 | 状态 |
|----|------|------|
| **线A** | 组会修复：预览表格上下滚动、structuredData JSON → 分区卡片、**首页看板丰富** | 本轮实施 |
| **线B** | AI应用：归一溯源 + 三级命中分布、AI质控解读卡、AI助手悬浮窗、质控评分真实化 | 本轮实施 |
| **公共能力** | **数据范围贯穿（DataScope）**：全部 / 筛选两种模式，贯穿清洗 / 重算 / 图谱 / 导出 / 统计 | 本轮实施 |
| **质控检验图谱** | 质控页 `Qc.vue` 图谱区：ECharts 2D 力导向，逻辑规则边 + 冲突边可视化 | 本轮实施 |
| **阶段0** | **基底质量**：既有代码修复（CSV/去重/计数/异常400/.xls/ObjectMapper/注册竞态）+ **RBAC 双角色落实** + 登录/注册页改版 | **先于阶段1** |
| **线C** | 科研评估：人工复核工作台 + AI初检vs人工复核一致性（Kappa）+ 质控页 + 实验报告产出 | **正式实施**（阶段9） |

### 1.2 阶段依赖

```
阶段0（基底质量 + RBAC权限 + 登录页，**前置**） → 阶段1-9 顺延执行
阶段1（线A：卡片v1） → 阶段2（线B-1：卡片v2 + 分布） → 阶段3（线B-2/3：解读卡 + 浮窗） → 阶段4（质控评分）
阶段5（范围贯穿）依赖阶段4（批量 batch 的 filters）
阶段6（质控图谱）依赖阶段4 LogicChecker + 阶段5 filters
阶段7（首页丰富）依赖阶段4 真实评分（评分分布）+ 阶段5 范围
阶段8：文档 + 回归（依赖 1-7 全部）；阶段9：线C 科研评估（依赖 4 评分 + 5 范围 + 6 质控页）
阶段0 的 `@RequireRole` 供阶段4 重算 / 阶段6 图谱 / 阶段9 复核复用权限。
```

**依赖说明**
- 阶段2 的溯源层级依赖阶段1 的卡片组件（v1 → v2）；
- 阶段3 的解读卡"归一命中"依赖阶段2 的 `normLevel`（缺省时降级只统计 content≠sourceText）；
- 阶段4 的 `stores/ai.js` 全量联调放在阶段3 之后（B3 需 `normByLevel` 与 `stats` 真实数据）；
- 阶段5 范围贯穿为公共能力，供阶段6（图谱）、阶段7（首页）及导出 / 预览复用；
- 阶段6 图谱的冲突红边依赖阶段4 的 LogicChecker；阶段7 的评分分布依赖阶段4 质控真实化；
- 阶段9 线C 依赖阶段4（评分生成待复核任务）、阶段5（范围）、阶段6（质控页），并依赖成员A 病历查询；
- 全链路现场演示（新病历 → 抽取 → 质控 → 图谱）依赖成员B 的 `/api/nlp/extract` 与 `PythonNlpClient`（成员B任务，见 3.2）；seed 数据为上游未就绪的过渡，口径统一"成员模块接入后即真实抽取"。

---

## 2. 详细任务

### 2.0 阶段0：基底质量与权限（先于阶段1）

> 目的：修复既有代码缺陷、落实双角色权限、统一登录/注册视觉，为阶段1-9 提供干净基底。

#### 2.0.1 既有代码修复

| # | 文件 | 改动 | 验证 |
|---|------|------|------|
| A1 | `GovernanceServiceImpl.toCsv` | `cols` 中 `"syndrome"`→`"pattern"`，header「证型」→「辨证结论」 | CSV 辨证结论列有值 |
| A2 | `GovernanceServiceImpl.clean` | "空值规整"计数改判 `v!=null && !v.isEmpty() && v.trim().isEmpty()` | 含空格字段病历清洗后计数>0 |
| A3 | `GovernanceServiceImpl.textHash` | 去重改 **MD5(joined UTF-8)**（替代 `hashCode`）；21字段补齐 `visitCount`/`visitTime` | 相同21字段判重复、不同不误杀 |
| A4 | `utils/request.js` | error 分支处理 401/403（清token跳登录 / 提示无权限） | 过期token请求→跳登录 |
| A5 | `DictionaryServiceImpl.parseFile` | `WorkbookFactory.create()` 替代 `XSSFWorkbook`（兼容 .xls） | 上传 .xls 导入成功 |
| A6 | `GlobalExceptionHandler` | 加 `HttpMessageNotReadable`/`MissingServletRequestParameter`/`MethodArgumentTypeMismatch` → 400 | 空body/坏JSON→400（非500） |
| C2 | 各 Controller | `operator()` 提 `common/utils/RequestUtils.currentUsername()` | 操作日志留痕正常 |
| C3 | 各 Service/Controller | 统一注入 Spring `ObjectMapper`（删 `new ObjectMapper()`，Stats 含时间模块） | 全量回归 |
| C4 | `AuthServiceImpl.register` | catch `DuplicateKeyException` → 400"用户名已存在"；补 `users.username` 唯一索引（database-init + ALTER） | 重复/并发注册→400 |

#### 2.0.2 RBAC 双角色落实

| 层 | 改动 |
|----|------|
| 后端 | 新增 `common/annotation/RequireRole.java` + `common/interceptors/RoleInterceptor.java`（读 request `currentRole`，未过 → HTTP 403 + Result）；`WebMvcConfig` 注册（JwtInterceptor 之后） |
| 标注 | **仅管理员**：dictionary/import、dictionary/rollback、governance/clean、governance/export、governance/preview、governance/stats、未来 qc/*；**登录即可**：terms/backups/normalize、stats/*、审计；**复核双角色**：线C 阶段9 的 review 接口 |
| 前端 | 路由 `meta.roles`（dashboard/review=双角色；治理/词典/审计/病历/质控=管理员）+ 守卫；`MainLayout` 改按登录 `menus` 渲染；`request.js` 加 403 提示 |
| 菜单差异 | 管理员 8 项全量；审核员=首页看板+人工复核（与 AuthServiceImpl 一致） |

#### 2.0.3 登录/注册页改版

| 项 | 改动 |
|----|------|
| 新建 `components/AuthShell.vue` | 登录/注册共用外壳：左品牌区（系统名/英文副题/细线墨点装饰/版本）+ 右表单 + 底部版权行 |
| 背景 | 宣纸 `--paper` + 极淡墨绿纹理，**无渐变/动画/emoji**；色值全走 theme 令牌 |
| `Login.vue` / `Register.vue` | 重写使用 AuthShell；保留校验/回车/autocomplete/密码可见；注册保留"角色=审核员"提示 |
| 验收 | 双页视觉统一、窄屏不破版、与站内中式主题一致 |

#### 2.0.4 文档同步

- 计划 D1：`stores/ai.js` → `store/ai.js`
- 计划 D2：§0 注明 `nlp.enabled` 默认 false，yml 可不显式配
- §8 新增「角色鉴权约定」（见 8.8）
- openapi：管理接口 description 标注"仅管理员"（可选）
- 功能设计文档：登录/注册页视觉小节（并入附录C）
- 词典导入：`convert`（LLM）/ JSON 直传 / 离线脚本 相关文档（openapi 同步）

#### 2.0.5 术语库导入增强（④ 混合方案：PDF+LLM / JSON 直传 / 离线脚本）

> 目标：**国标 PDF 免手工转 JSON**，兑现功能设计第16节「LLM 即 ETL」；断网仍可走离线路径。

| 子项 | 做法 |
|------|------|
| 系统主链路 | `POST /api/dictionary/convert`：上传 `.pdf` → PDFBox 抽文本 → 调 LLM 转换（Prompt 提取 standardTerm/aliases，仅返回 JSON，同文档16节）→ 返回候选条目预览 → 管理员确认 → `POST /api/dictionary/import` 写入 |
| JSON 直传 | `import` 支持 `.json`（TermEntry 数组 `[{standardTerm, aliases[], source?}]`，校验+去重合并别名）；前端 `accept=".xlsx,.xls,.csv,.json"` |
| LLM 通道 | 新增 `common/utils/LlmConversionClient.java`（复用 PythonNlpClient 式 HTTP 封装，指向 NLP 服务 `/api/nlp/convert`）；开关 `nlp.convert-enabled` 默认 false；关闭时 PDF 上传返回友好提示（走离线脚本 / JSON 直传） |
| 离线脚本 | `tools/convert-standard-pdf.py`（python pdfplumber 抽表格 → 组 standardTerm/aliases/source → 出 5 类 JSON → 打印校对清单）；老师提供真实国标 PDF 时跑一次，产物可直接 JSON 直传 |
| 依赖 | 后端加 `org.apache.pdfbox:pdfbox`；脚本需 Python `pdfplumber` |
| 预览确认 | `ConvertPreviewVO {type, candidates:[{standardTerm, aliases, source}], failed}`；确认后再 import，失败明细可下载 |
| 验收 | LLM 开关开：PDF→预览→确认入库全通；JSON 直传成功；断网（开关关）：PDF 兜底提示；.xls 复验通过 |

#### 2.0.6 体验微修复

| # | 问题 | 修法 | 验证 |
|---|------|------|------|
| U1 | 清洗 / 词典导入无二次确认（一键全库 / 覆盖+备份） | 加 `ElMessageBox.confirm`，文案带影响范围预判 | 误触可取消 |
| U2 | `TermInput` 自动补全每击键发请求、旧响应覆盖新结果 | debounce 200ms + 空串不发 + latest-wins | 快速输入只发有效请求 |
| U3 | `Dictionary` 查询无加载态；`Dashboard` 整页白屏刷新 | table 加 `v-loading`；Dashboard 局部刷新态 | 操作有即时反馈 |
| U4 | 图表 / 表格空数据无占位 | 统一 `el-empty` 空态文案 | 空数据有交代 |
| U5 | 全库操作不预告影响面 | 确认弹窗带"将处理 全部 / 子集 n 条" | 操作前后预期一致 |
| U7 | 无 favicon | 中式墨绿 favicon（静态文件 + link） | 浏览器标签有图标 |
| U8 | `AuditLog` mock 无标识 | `USE_MOCK` 时顶部状态条"演示数据 · 等待成员A 接口" | 演示不误解 |
| U13 | 登录页无演示账号提示 | AuthShell 底部小字显示 admin / auditor 账号 | 访客可自助登录 |
| U14 | 注册用户名无格式约束 | `RegisterDTO @Size(2..20)` + 正则；后端同步校验 | 非法格式→400 |
| U15 | 操作日志相对路径（随启动目录漂移） | `log.operation-file` 改绝对（`data/logs/operation.log`）+ README 说明 | 任意目录启动日志稳定 |

**验收标准**：`mvn test`（新增异常400/注册竞态/角色403 用例）+ `npm run build` + 双角色菜单差异、直输 URL 被拒、登录/注册观感、CSV 证型列、清洗空值计数、坏 JSON→400、**词典 PDF 转换/JSON 直传/断网兜底**。

### 2.1 线A：组会修复

#### A1 预览表格上下滚动修复

| 项 | 明细 |
|----|------|
| 文件 | `frontend/src/views/Governance.vue:111` |
| 改动 | `max-height="300"` → `max-height="520"` |
| 细节 | ① 10条样本+表头≈450px，520px 内基本一屏<br>② 保留固定列（登记号/评分/分级）与横向滚动<br>③ 空态：`total=0` 时沿用现有 warning 提示<br>④ 溢出列 `show-overflow-tooltip` 保留 |
| 边界 | 后端 preview 样本上限=10 不动；页面整体高度略增，无布局溢出风险 |

**验收标准**：预览 10 条样本一屏显示，无需上下拖动；横向滚动与固定列正常。

#### A2 structured_data 分区卡片（v1：原文 ↔ 标准词，无层级）

| 项 | 明细 |
|----|------|
| 新建 | `frontend/src/components/StructuredDataCard.vue` |
| 输入 | `structuredData`（JSON字符串或对象，结构见下） |
| 分区 | 9类（疾病/症状/舌象/脉象/证候/病因/治法/方剂/中药），**空区自动隐藏**（当前 `causeList`、`treatmentList` 为空数组时不显示） |
| 条目渲染 | 实体类：「**标准词**」+ 小字「原文：sourceText」；中药类：「名称 + 剂量」+ 小字原文 |
| 溯源标记(v1) | `content ≠ sourceText` 时显示小斜线icon，`el-tooltip`：「原文 → 标准词」（层级字段 v2 补） |
| 样式 | 中式变量（--paper/--line/--ink）；分区标题 3px 竖线；条目浅底卡片 |
| 空态 | 无结构化数据 → 「无标准化数据」占位；异常JSON → 原样降级展示 |
| 应用点 | 替换 `Governance.vue:153` 的 `<pre class="sd-json">`，详情弹窗复用 |
| 复用性 | 未来 Review 页 / 详情页共用 |

**输入结构示例（附录A）**
```json
{
  "diseases": [{"content": "感冒", "sourceText": "感冒"}],
  "symptoms": [{"content": "头痛", "sourceText": "头部胀痛"}],
  "tongueList": [{"content": "舌淡红", "sourceText": "舌淡红"}],
  "pulseList": [{"content": "脉浮", "sourceText": "脉浮"}],
  "patternList": [{"content": "风寒感冒", "sourceText": "风寒感冒"}],
  "causeList": [], "treatmentList": [],
  "formulaList": [{"content": "桂枝汤", "sourceText": "桂枝汤"}],
  "herbs": [{"name": "桂枝", "dosage": "10g", "sourceText": "桂枝10g"}]
}
```

**验收标准**：9 类分区正确渲染（病因/治法自动隐藏）；中药含剂量；`content≠sourceText` 的条目显示溯源标记；空数据与异常 JSON 有兜底。

---

### 2.2 线B-1：归一溯源与三级命中分布

**后端**

| 文件 | 改动 |
|------|------|
| `common/utils/EsTermNormalizer.java` | `NormalizeResult` 增加 `int level`（1=精确 / 2=包含 / 3=模糊）；适配现有调用点（`GovernanceController.normalize` 单条透出 level） |
| `service/impl/GovernanceServiceImpl.java` | 归一循环写 `entity.put("normLevel", level)`、`put("normSource", source)`；全局累计 `Map<String,Integer>` → 返回 |
| `domain/vo/CleanResultVO.java` | 加 `normByLevel {exact, contain, fuzzy}` |
| 数据 | 重跑一次 `/api/governance/clean`，使记录带上 normLevel |

**前端**

| 组件 | 改动 |
|------|------|
| `StructuredDataCard.vue` v2 | 溯源 tooltip 升级「原文 → 标准词 【命中层级：精确/包含/模糊】」；`normLevel` 缺失时降级 v1（前后端解耦） |
| `Governance.vue` 清洗结果区 | 六宫格下方新增**三级命中分布**（精确 n · 包含 n · 模糊 n 三细柱/数），数据来自 `clean.result.normByLevel`；接口未返回时隐藏（渐进增强） |

**验收标准**：清洗后分布区显示精确/包含/模糊计数；卡片溯源提示含命中层级；`normLevel` 缺失时页面不报错、降级 v1。

---

### 2.3 线B-2：AI质控解读卡

| 项 | 明细 |
|----|------|
| 触发 | 详情弹窗底部「AI质控解读」按钮 → 生成区展开（skeleton 1秒模拟分析） |
| 数据源 | 行21字段 + structuredData + normLevel（**前端规则计算，零后端依赖**） |
| 报告模板 | ① 完整性：21字段缺失清单<br>② 缺项重点：核心字段（脉象/舌象/证候/治法/方剂/中药）缺失<br>③ 归一命中：n 处归一（各层计数）<br>④ 关键提示：如"辨证结论为空""处方缺失"<br>⑤ 声明：`AI辅助分析，最终以人工复核为准` |
| 空态 | 无 structuredData → 仅出完整性分析 |
| 样式 | 中式卡片，浅纸底，结论区 + 列表；克制不 AI 感 |

**验收标准**：点击按钮出骨架屏后展示完整报告；缺核心字段时给出明确提示；含"AI辅助·人工兜底"声明。

---

### 2.4 线B-3：AI助手悬浮窗（全局）

| 项 | 明细 |
|----|------|
| 新建 | `frontend/src/components/AiAssistant.vue`；挂 `layouts/MainLayout.vue`（所有登录页可用） |
| 悬浮球 | 右下角中式圆钮（墨绿·药丸icon）→ 展开/收起面板（约 360×480） |
| 面板结构 | 顶部标题「AI 助手」+ 快捷指令 chips + 自由输入框 + 回答区（支持列表渲染） |
| 快捷指令 | 分析当前病历 / 质控合格率构成 / 归一命中分布 / 阈值敏感性实验 / 我可以做什么 |
| 规则问答路由 | `记录数/合格/待复核/无效` → stats<br>`归一/分布` → normByLevel<br>`当前病历` → 活跃详情（复用解读卡模板）<br>兜底 → 「该问题建议人工复核完成，AI不替代诊断」 |
| 状态共享 | 新增 `frontend/src/stores/ai.js`（pinia）：`activeRecord`（Governance 详情点击写入）、`normByLevel`（最近 clean 写入）、`stats`（治理状态行同步）；无数据时指令降级提示 |
| 阈值敏感性实验面板 | 相似度阈值 0.6/0.7/0.8/0.9 → 模糊命中模拟曲线（内嵌**演示数据**），标注"参数敏感性实验·演示"；注明"真实实验数据由后端归一统计接口出具（后续阶段）" |
| 约束 | 单轮问答（无会话历史）；`z-index` 置顶不遮菜单；样式保持页面主题 |

**验收标准**：五条快捷指令均返回可读回答；未打开详情时"分析当前病历"有降级提示；阈值实验面板可出曲线且带"演示"标注。

---

### 2.5 线B-4：AI质控评分（分数真实化）

**后端算法**

| 模块 | 设计 |
|------|------|
| `common/utils/QcScorer.java` | **判空规则**：核心字段按 structured_data 非空判断<br>structured 为空再回退查原始列（pulse/tongue/pattern/prescription）<br>两者皆空才判缺失 |
| 三层扣分 | ① 核心缺失各 -15（脉象/舌象/证候/治法/方剂/中药，共6项）<br>② 逻辑冲突各 -10（证候-治法 / 证候-方剂 / 证候-中药）<br>③ 格式+重复+症状为空 各 -5（age 非数字 / 性别非男女 / 文本MD5重复 / symptoms 为空） |
| 严重判定 | 核心缺失 ≥3；证候-治法与证候-方剂同时冲突；文本无法解析 → 直接 invalid；`score = max(0)` |
| 分级路由 | ≥90 且无逻辑冲突 → 合格<br>60~89 分或存在逻辑冲突 → 待复核<br>60 分以下或严重问题 → 无效 |
| `common/utils/LogicChecker.java` | 证候→治法→方剂规则（固定写死、不支持自编辑）：<br>· 风寒感冒 → 辛温解表 → 麻黄汤/桂枝汤<br>· 风热感冒 → 辛凉解表 → 银翘散/桑菊饮<br>· 暑湿感冒 → 清暑祛湿 → 新加香薷饮<br>舌脉规则：舌红×脉沉迟、舌淡×脉数 → conflict<br>**未覆盖证候不判冲突（不误杀）** |

**接口**

| 接口 | 说明 |
|------|------|
| `POST /api/qc/score` | 单条评分，入参 `QcScoreDTO`（结构化数据），返回 `ScoreResultVO` |
| `POST /api/qc/score/batch` | 全库重算（事务 + 逐条幂等），返回分级分布；批量前清理旧 review_tasks(pending) |

**`ScoreResultVO` 与 `qc_results` 写入结构（可溯源）**
```json
{
  "score": 75,
  "grade": "待复核",
  "deductions": [
    {"type": "核心字段缺失", "item": "脉象", "points": 15, "reason": "pulseList 与 pulse 均为空"},
    {"type": "逻辑冲突", "item": "证候-方剂", "points": 10, "reason": "风寒感冒 不匹配 银翘散"}
  ],
  "logicConflicts": ["证候-方剂不匹配：风寒感冒 × 银翘散"],
  "serious": false,
  "checkedAt": "2026-09-17 10:00:00"
}
```

**前端重算入口**

| 项 | 明细 |
|----|------|
| 位置 | `Governance.vue` 治理状态行右侧「**质控评分重算**」按钮（仅管理员可见） |
| 交互 | 点击 → confirm（"将按质控规则对全库重算评分与分级，覆盖现有分数，确认？"）→ loading → 提示新分布 → 刷新治理状态行 + 看板 |
| 失败态 | 401/500 统一错误提示；接口未开通时按钮置灰 + tooltip"后端预留" |
| mock 形态 | 审核阶段按钮先用假分布演示（真实接口由后端提供） |

**与现有链路联动**
- 重算后看板 overview 数字自动正确；导出/预览只导"真实合格"（范围变化属预期）；`governed` 保持现状不重置；
- 待复核病历自动生成 `review_tasks`（pending，deadline=+7 工作日）到数据层（UI 属线C待定）；
- 状态一致性（U16）：待复核病历 `records.status` 同步置 `reviewing`，避免看板 `pendingReviewCount(status)` 与"待复核"口径不一致；
- 性能：500 条内存循环 + 词典查询，预计 1s 内；重复调用结果一致（幂等）。

**验收标准**：batch 后分级分布合理（非 85/12/3 随机）；抽查 `qc_results` 扣分明细与 score 一致；border 例（60/90 分）路由正确。

---

### 2.6 线C：科研评估（正式实施）

#### 2.6.1 复核与一致性

| 模块 | 设计要点 |
|------|---------|
| Review 工作台 `Review.vue`（审核员） | 待复核列表（7工作日截止高亮）+ 左右对照（原始病历 / AI预检单 + 结构化数据）→ 修正·复核通过·保持待复核（自动重算） |
| 质控页 `Qc.vue`（管理员） | 批量重算入口 + AI预检列表/扣分明细 + 一致性统计卡（Kappa/混淆矩阵） |
| `GET /api/qc/agreement` | 一致性统计：AI初评 vs 人工复核 → 吻合率 + Cohen's Kappa + 三级混淆矩阵 |
| DB `review_tasks` 扩展列 | `ai_grade` / `manual_grade` / `reviewed_by` / `completed_time`（ALTER TABLE + database-init.sql 同步） |
| 接口契约 | `GET /api/review/tasks`、`POST /api/records/{recordId}/review`（openapi 已定义，按契约实现） |

#### 2.6.2 实验产出（拿更高奖的关键）

| 实验 | 做法 | 产出 |
|------|------|------|
| 人机一致性实验 | 找 2~3 人模拟审核员，核对至少 **20 份待复核病历** | 吻合率 + **Cohen's Kappa** + 三级混淆矩阵 |
| 归一/阈值实验 | 三级命中分布（精确/包含/模糊占比）+ 相似度阈值（0.6~0.9）敏感性曲线 | 图表 + 统计结论 + 局限（如"Dice≥0.8 时模糊命中 x%、误归一 y 例"），**可复现** |

- 报告形态：质控页一致性统计卡 + 可导出的实验结论页；
- 结论要求：数据 → 图表 → 结论 → 局限，评审可追责。

---

### 2.7 数据范围贯穿（DataScope）

> 理念：**先选范围 → 常驻"当前范围条" → 后续操作只作用于范围内数据**；不设条件时 = 全部病历。

| 项 | 明细 |
|----|------|
| 两种模式 | **全部病历**（范围条不设条件，默认）；**部分数据**（按 科室 / 时间 / 证候 / 分级 任意组合筛选出的子集） |
| 前端 | `StatsFilter.vue` 升级为 `RangeFilter`（科室 / 时间区间 / 证候 / 分级 grade 4维）；各操作页上方常驻**当前范围条**（"当前范围：全部" 或 "中医内科 · 2026-01-01~06-30 · 合格（共 n 条）"） |
| 后端 | `StatsServiceImpl.filterByFilters` 提为**公共工具**（新增 grade 条件）；各 DTO 统一 `filters{department, dateRange, pattern, grade}` |
| 接入操作 | 看板统计（已有）<br>导出 / 预览（已有）<br>**清洗 clean**（改：默认全部，可传 filters 只清洗范围内合格病历）<br>**质控重算 batch**（改：默认全部，可传 filters 范围内重算）<br>**图谱 graph**（范围内聚合，见 2.9）<br>首页统计扩展（见 2.8） |
| 口径 | **所见即所操作**：每次操作结果标注作用范围与影响条数；范围条为空 = 全部，结果与全库一致；带范围 = 仅子集 |

- **科室动态（U11）**：`RangeFilter` 的科室选项改为后端动态获取（`GET /api/stats/departments`，`records.department DISTINCT`），替代前端硬编码（解决 StatsFilter / Governance 两处写死不统一）；
- **补全筛选控件（U12）**：后端已支持 pattern / grade，UI 打开"证候 / 分级"两维，一并解放"按证候看分布"能力。

**验收标准**：任意页面改动范围为子集后，清洗 / 重算 / 图谱 / 导出作用对象与范围条一致；范围条为空时结果与全库一致。

---

### 2.8 首页看板丰富

| 区块 | 数据源 |
|------|--------|
| **待办快捷条**（待复核 / 待治理 → 点击跳质控/清洗页） | overview + governanceStats（已有） |
| **门诊量趋势**（按月折线，跨整行） | 新增 `GET /api/stats/extra`（visit_time 按月聚合） |
| **科室分布与合格率**（横条） | 新增聚合（department 分组 count + 合格率） |
| **治疗效果分布**（饼图） | 新增聚合（treatment_effect） |
| **评分分布直方图**（90+/80~89/70~79/60~69/60以下） | 新增聚合（阶段4 质控真实化后生效，之前隐藏该卡） |
| **词典规模卡**（5类术语数量） | 已有词典接口 |

布局：现有4图保留下移；待办条置顶；趋势图跨整行；新建分布图与现有图组成 3 行对称。**前端先行 mock 审核 → 后端 `GET /api/stats/extra`**。

**补充**：① 请求合并（U6）：看板一次查询改单接口 `GET /api/stats/all`（overview + 4 类统计一次返回），消除 5 个并发请求；② 响应式（U17）：`grid-2` 在 1200px 断点折叠为单列（与其它页面断点一致）。

**验收标准**：首页信息密度明显提升（6+ 区块）；趋势 / 科室 / 效果 / 评分分布正确且随范围条联动；待办点击可跳转。

---

### 2.9 质控检验图谱（Qc.vue）

| 项 | 明细 |
|----|------|
| 位置 | 质控校验页 `Qc.vue`（当前占位）图谱区；质控页其余内容仍留线C |
| 数据接口 | `GET /api/qc/graph`（全库 / 范围内聚合，复用 2.7 filters） |
| 节点 | 病历（墨绿圆点、size=1）+ 实体（disease / pattern / symptom / tongue / pulse / formula / herb，按类型着色、size=频次） |
| 边 | ① 病历 → 实体 关联边<br>② 证候 → 治法/方剂（LogicChecker 规则子图）<br>③ 冲突边（checkLogic 命中 → 红色虚线 + tooltip 原因） |
| 布局 | ECharts `graph` 系列 **2D 力导向**（零新增依赖，echarts 已内置）；roam 拖拽/缩放；点击节点 `focusAdjacency` 高亮邻居；节点按频次 topN 截断（默认 200）+ 图例 + 空态 |
| 数据边界 | 当前 `treatmentList=[]`、"方剂-中药"无结构化归属 → 图谱以"病历-实体"星型为主骨架，规则子图覆盖 LogicChecker 3 条证候（**不制造假关系**） |
| 工作流 | 前端 mock 图数据先行审核 → 后端聚合接口 |

**验收标准**：整体 / 筛选图谱正确渲染；冲突边红色可溯源；点选聚焦可用；topN 截断性能正常。

---

## 3. 成员A/B任务（协作，与本方案并行推进）

### 3.1 成员A（Java + 前端）：病历数据与审计

| 任务 | 对应必做 | 现状 | 备注 |
|------|---------|------|------|
| 病历批量导入 / 单条新增 / 进度查询 | 必做3 | RecordController 未实现 | `POST /api/records/import`<br>`POST /api/records`<br>`GET /api/records/import/{taskId}/status`<br>导入任务状态内存 Map（≤100 条可用） |
| 原始病历查看 | 必做4 | 未实现 | `GET /api/records/raw/{recordId}`（只读） |
| 病历修改 / 删除 / 标签 / 归档 | 必做5 | 未实现 | 原始21字段只读，仅允许修正 structuredData / qcResults；`PUT /api/records/{recordId}`、`DELETE /api/records` |
| 多条件病历查询 | 查询1 | 未实现 | `GET /api/records/search`；MyBatis-Plus `selectPage` 分页插件可直接使用；复用 filters（department/dateRange/pattern） |
| 日志审计（后端） | 必做2 | LogController 未实现 | `GET /api/logs/export`（文件流不套 Result）；OperationLogger 已接入 clean/export/import/rollback |
| 前端「病历数据」页 + 审计页 | 必做3-5、查询1、必做2 | Placeholder 占位 / AuditLog.vue 为 mock | AuditLog.vue 切真接口（成员A 的 LogController）；病历数据页可复用 Entity/Herb 组件 |

### 3.2 成员B（Python + Java + 前端）：NLP、质控、复核

| 任务 | 对应必做 | 现状 | 备注 |
|------|---------|------|------|
| NLP 实体抽取（Python） | 必做6-9 | PythonNlpClient 已封装（`nlp.enabled=false`） | 启动 Python NLP 服务后置 true；一次返回完整 structuredData |
| 质控检查（缺失/格式/查重） | 必做11-12 | `POST /api/qc/check` 契约已定义 | 按 openapi 契约实现 |
| 诊疗逻辑一致性 | 拓展1 | `POST /api/qc/check/logic` 契约已定义 | 与 B4 的 LogicChecker 共用规则表 |
| 质控评分落地 | 必做15 | **本方案 2.5（阶段4）** | QcScorer + LogicChecker + `/api/qc/score`、`/api/qc/score/batch` |
| 人工复核工作台 | 必做13 | **线C（阶段9，正式）** | Review.vue + `GET /api/review/tasks` + `POST /api/records/{recordId}/review` |
| 前端「结构化解析 / 质控校验 / 人工复核」页 | 必做6-9/15/13 | Placeholder 占位 | 结构化解析页原文对照；质控校验页展示评分/扣分（线C 的 Qc.vue）；复核页见线C |

### 3.3 协作衔接边界

- **质控评分（B4）**：依赖成员C 的词典与结构化数据（附录A），前端重算入口由成员C 出；
- **复核（线C）**：依赖成员A 的病历查询（按条件筛选待复核）与成员B 的评分结果；
- 所有接口统一 Result 包装、遵循 openapi 契约；文件流接口（日志导出）不套 Result。

---

## 4. 实施顺序（前端先行）

| 阶段 | 内容 | 前端先行 | 审核要点 | 后端落地 | 验证 |
|------|------|---------|---------|---------|------|
| 0 | 基底+权限+登录页 | 登录/注册 AuthShell（mock） | 双页观感；菜单差异 | A1-A6 + C2-C4 + @RequireRole | mvn test；build；双角色直输URL被拒 |
| 0B | 术语库导入增强（④混合） | Dictionary.vue PDF/JSON 导入（mock） | PDF 预览→确认流程 | convert + JSON 直传 + pdfbox | build+手工；转换预览/兜底 |
| 1 | 组会修复 | A1 预览高 520 + A2 卡片 v1 | 预览一屏 10 条；卡片观感/空态/溯源标记 | — | build + 手工 |
| 2 | 溯源 + 分布 | 卡片 v2 + 清洗分布区 | 溯源层级样式；分布区布局 | normLevel + normByLevel + CleanResultVO | mvn test；重跑 /clean |
| 3 | 解读卡 + 浮窗 | B2 + B3 | 解读卡报告；浮窗交互/五指令 | —（store 联调放阶段4） | build + 手工 |
| 4 | 质控评分 | 重算按钮 + mock 分布 | 按钮交互/确认/loading | QcScorer + LogicChecker + QcController + batch | mvn test；分布真实；看板/导出联动 |
| 5 | 范围贯穿 | RangeFilter + 当前范围条（mock） | 范围条文案；筛选交互 | filterByFilters 提公共 + clean/batch 接 filters | 手工：子集清洗/重算作用对象正确 |
| 6 | 质控图谱 | Qc.vue 图谱区（mock 图数据） | 图谱观感；冲突红边；交互 | `GET /api/qc/graph` 聚合接口 | build + 手工；后端聚合单测 |
| 7 | 首页丰富 | Dashboard 新布局（mock 数据） | 新区块观感；随范围联动 | `GET /api/stats/extra` 聚合 | build + 手工；聚合单测 |
| 8 | 文档 + 回归 | — | — | — | 全套回归 |
| 9 | 线C 科研评估 | Review.vue + 质控页一致性卡（mock） | 复核界面；一致性展示 | ReviewController + agreement + 模拟复核 | 模拟复核≥20条算Kappa；实验报告产出 |

---

## 5. 文件变更清单

### 5.1 后端

| 阶段 | 文件 | 说明 |
|------|------|------|
| 0 | `GovernanceServiceImpl.java` | 修复 CSV 证型列 / 空值计数 / 去重 MD5 |
| 0 | `common/annotation/RequireRole.java`（新增） | 角色注解 |
| 0 | `common/interceptors/RoleInterceptor.java`（新增） | 角色校验（403） |
| 0 | `common/utils/RequestUtils.java`（新增） | 当前操作人公共方法 |
| 0 | `GlobalExceptionHandler.java` | 增加 400 异常分支 |
| 0 | `AuthServiceImpl.java` | 注册竞态 catch；users.username UNIQUE（ALTER） |
| 0 | `pom.xml` | 加 `org.apache.pdfbox:pdfbox` |
| 0 | `common/utils/LlmConversionClient.java`（新增） | PDF 文本→LLM 转换调用（`nlp.convert-enabled` 开关） |
| 0 | `controller/DictionaryController.java` | 增 `/api/dictionary/convert`（PDF→预览） |
| 0 | `service/impl/DictionaryServiceImpl.java` | import 支持 `.json` 直传；convert 流程 |
| 0 | `domain/vo/ConvertPreviewVO.java`（新增） | 转换预览结果 |
| 0 | `tools/convert-standard-pdf.py`（新增） | 离线 PDF→JSON 脚本 |
| 2 | `common/utils/EsTermNormalizer.java` | 加 `level` 字段 |
| 2 | `service/impl/GovernanceServiceImpl.java` | 归一循环写 normLevel + 累计 |
| 2 | `domain/vo/CleanResultVO.java` | 加 `normByLevel` |
| 4 | `common/utils/QcScorer.java`（新增） | 三层扣分 + 严重判定 + 分级 |
| 4 | `common/utils/LogicChecker.java`（新增） | checkLogic 规则 |
| 4 | `controller/QcController.java`（新增） | POST /qc/score、/qc/score/batch |
| 4 | `service/IQcService.java` + `impl/QcServiceImpl.java`（新增） | 单条/批量评分 |
| 4 | `domain/dto/QcScoreDTO.java`（新增） | 入参 |
| 4 | `domain/vo/ScoreResultVO.java`（新增） | 单条评分结果 |
| 4 | 测试：`QcScorerTest.java`、`LogicCheckerTest.java` | 单元用例矩阵 |
| 5 | `common/utils/RecordFilter.java`（新增） | filterByFilters 提公共（+grade 条件） |
| 5 | `service/impl/GovernanceServiceImpl.java` | clean 支持 filters（范围内清洗） |
| 5 | `service/impl/QcServiceImpl.java` | batch 支持 filters（范围内重算） |
| 6 | `controller/QcController.java` | 增 GET /api/qc/graph |
| 6 | `domain/vo/GraphVO.java`（新增） | 图谱数据：records / edges |
| 6 | `service/impl/QcServiceImpl.java` | 图谱聚合（节点=病历+实体，边=关联/规则/冲突） |
| 7 | `controller/StatsController.java` | 增 GET /api/stats/extra |
| 7 | `domain/vo/StatsVO.java` / `OverviewVO.java` | 新增聚合口径（trend / department / effect / scoreDist） |
| 9 | `controller/ReviewController.java`（新增） | GET /api/review/tasks、POST /api/records/{recordId}/review |
| 9 | `service/IReviewService.java` + `impl/ReviewServiceImpl.java`（新增） | 复核 + 自动重算回流 |
| 9 | `controller/QcController.java` | 增 GET /api/qc/agreement（Kappa/混淆矩阵） |
| 9 | 测试：`ReviewServiceTest.java`、`AgreementTest.java` | 复核流转 / Kappa 计算用例 |

### 5.2 前端

| 阶段 | 文件 | 说明 |
|------|------|------|
| 0 | `components/AuthShell.vue`（新增） | 登录/注册统一外壳 |
| 0 | `views/Login.vue` / `views/Register.vue` | 重构使用 AuthShell |
| 0 | `utils/request.js` | 401 / 403 统一处理 |
| 0 | `layouts/MainLayout.vue` | 按登录 menus 渲染 |
| 0 | `router/index.js` | meta.roles + 守卫 |
| 0 | `views/Dictionary.vue` | accept 增 `.pdf,.json`；PDF 转换预览+确认导入流程 |
| 0 | `api/dictionary.js` | convert 调用 |
| 1 | `components/StructuredDataCard.vue`（新增） | 分区卡片 v1 |
| 1 | `views/Governance.vue` | max-height→520；`<pre>` → 组件 |
| 2 | `components/StructuredDataCard.vue` | v2 加层级 tooltip |
| 2 | `views/Governance.vue` | 清洗结果区加三级分布 |
| 3 | `components/AiAssistant.vue`（新增） | 全局悬浮窗 |
| 3 | `layouts/MainLayout.vue` | 挂悬浮窗 |
| 3 | `stores/ai.js`（新增，pinia） | 状态共享 |
| 4 | `views/Governance.vue` | 重算按钮（管理员可见） |
| 4 | `api/governance.js` | 新增重算接口调用 |
| 4 | `stores/ai.js` 联调 | 与后端同步 |
| 5 | `components/RangeFilter.vue`（StatsFilter 升级/新增） | 4维范围筛选 + 当前范围条 |
| 5 | 各操作页（Governance / Qc / Dashboard / 导出） | 接入 RangeFilter + 范围条 |
| 6 | `views/Qc.vue`（新增） | 质控页图谱区（ECharts graph，先 mock） |
| 6 | `api/qc.js`（新增） | graph 调用 |
| 6 | `data/graph-mock.js`（新增） | 图谱 mock 数据 |
| 7 | `views/Dashboard.vue` | 新布局（待办条/趋势/科室/效果/评分分布/词典卡） |
| 7 | `api/stats.js` | extra 调用 |
| 9 | `views/Review.vue`（新增） | 复核工作台（列表 + 左右对照） |
| 9 | `views/Qc.vue` | 质控页其余：预检/扣分明细/一致性卡 |
| 9 | `api/review.js`（新增） | review/tasks、records/{id}/review 调用 |
| 9 | `api/qc.js` | agreement 调用 |

### 5.3 数据库（阶段9）

- `review_tasks` 加列：`ai_grade` / `manual_grade` / `reviewed_by` / `completed_time`（ALTER TABLE；database-init.sql 同步）

### 5.4 文档（阶段8）

- `docs/功能设计文档.md`：附录C AI应用功能设计
- `docs/项目设计文档.md`：第14节 AI可解释质控增强设计
- `docs/中医电子病历质控与标准化系统-openapi.yaml`：补接口 + clean 响应字段
- `docs/需求分析报告.md`：补充 AI 应用需求条目
- `docs/交付报告.md`：每阶段完成后追加对应章节（只记录已完成）

---

## 6. 验证与回归

### 6.1 后端单测（阶段2 / 4）

- **QcScorerTest**：6字段全空 → -90 → invalid；仅脉象缺失 → 85 → 待复核；90分无冲突 → 合格；核心缺3项 → 直接 invalid；证候-治法+证候-方剂同时冲突 → 严重
- **LogicCheckerTest**：风寒感冒+辛温解表+麻黄汤 → consistent；风寒感冒+辛凉解表 → conflict；舌红+脉沉迟 → conflict
- 既有 14 例适配后通过

### 6.2 前端构建（每阶段）

- `npm run build` 无报错

### 6.3 手工回归

| 场景 | 预期 |
|------|------|
| 角色差异 | 审核员登录仅见 首页+复核；直输 /governance 被拒（403 提示） |
| 登录/注册页 | 双页视觉统一、宣纸底、无渐变；窄屏正常 |
| CSV 证型列 | 导出 CSV「辨证结论」列有值 |
| 清洗空值规整 | 含空格字段病历清洗后 空值规整>0 |
| 坏 JSON/空 body | 返回 HTTP 400（非 500） |
| 词典 PDF/JSON 导入 | PDF+LLM（开关开）返回候选预览→确认入库；JSON 直传成功；断网（开关关）PDF 兜底提示 |
| 预览数据集 | 10条样本一屏显示，无需上下拖动 |
| 分区卡片 | 9类分区正确（病因/治法自动隐藏），中药含剂量，溯源标记出现 |
| 清洗三级分布 | 执行 clean 后分布区显示精确/包含/模糊计数 |
| AI质控解读 | 点击按钮 → 骨架屏 → 报告（完整性/缺项/归一统计） |
| AI助手悬浮窗 | 五条快捷指令可答；阈值实验曲线展示；兜底话术 |
| 质控重算 | 点击 → 确认 → loading → 成功提示 → 看板数字变化 → 导出范围变化 |
| 批量重算后 | 分布真实（非85/12/3随机）；抽查 qc_results 扣分明细正确 |
| 范围内清洗/重算 | 选子集（如"中医内科 · 合格"）后操作，作用条数与范围条一致；范围条=全部时与全库一致 |
| 质控检验图谱 | 全库/筛选图正确渲染；冲突边红色可溯源；点选聚焦；空态提示 |
| 首页看板 | 6+ 区块渲染；趋势/科室/效果/评分随范围条联动；待办点击跳转 |
| 人工复核/Kappa | 模拟复核≥20条后，一致性卡显示吻合率 + Kappa + 混淆矩阵；实验结论页可导出 |
| 任务互斥 | 连续快速点击"质控评分重算"两次，第二次提示"任务进行中"，不重复执行 |

---

## 7. 注意点

| 注意点 | 处理 |
|--------|------|
| 重算后合格数量可能剧变（441 大跌） | 属预期；口径统一"评分规则真实化" |
| checkLogic 规则仅3条（覆盖率窄） | 写死不支持自编辑；未覆盖证候不判冲突（不误杀） |
| 前端 mock 与后端真数据不一致 | 渐进增强：无接口/字段时自动降级隐藏 |
| 悬浮窗遮挡操作 | 置顶但不遮菜单；收起后不挡交互 |
| 线C 需 review_tasks 扩展列 | ALTER TABLE 延到线C 实施时执行 |
| 范围内操作的影响面 | 操作前范围条必展示；结果含"影响条数"，避免误操作 |
| 图谱节点性能 | topN 截断默认 200；超出提示"仅展示高频节点" |
| 首页评分分布依赖真实评分 | 阶段4 质控真实化前隐藏该卡，避免展示假分数 |
| Kappa 需人工参与 | ≥2 人 · ≥20 条才可计算；小样本注明置信局限 |
| NLP 上游依赖成员A/B | 全链路现场演示依赖 `/api/nlp/extract` + PythonNlpClient(enabled=true)；seed 为过渡，口径"成员接入后即真实抽取" |
| 任务并发互斥 | batch / clean 用 Redis SETNX 防连点/并发触发；词典导入/回滚用 JVM 锁 |
| 双角色差异以文档为准 | 管理员/审核员菜单与接口权限见 §8.8；演示可全量菜单，但后端强制拦截越权 |
| 登录页风格统一 | 登录/注册共用 AuthShell，宣纸底、不退回渐变 |
| LLM 转换依赖外部 | `nlp.convert-enabled` 默认 false；关闭时 PDF 走离线脚本 / JSON 直传 |
| PDF 解析质量 | 受国标版式影响；转换产物走"预览确认 + 批量校对"兜底，不直接入库 |
| 静态部署 history 404（U9/U18） | 演示走 vite dev（3000）；如打包上 nginx 需 `try_files` fallback + README 说明 |
| Dashboard 窄屏（U17） | 阶段7 加 1200px 断点将 grid-2 折叠为单列 |

---

## 8. 开发规范与协作

### 8.1 后端规范

- **分层**：`common`（config/domain/exception/interceptors/utils）+ `controller` + `domain`（dto/po/vo）+ `mapper` + `service`（接口）+ `service/impl`（实现）；
- **命名**：`I*Service` 接口 + `*ServiceImpl` 实现（`@Service`）；绑表服务 `extends IService<Po>` / `ServiceImpl<Mapper,Po>`；
- **响应**：统一 `Result{code,msg,data}`，文件流接口（导出/日志）除外；
- **异常**：业务异常由 `GlobalExceptionHandler` 统一出口（401 / 400 / 500 → Result），**Controller 禁止 try-catch 兜错**；
- **留痕**：清洗 / 导出 / 导入 / 回滚 / 复核等关键操作调用 `OperationLogger` 写 `logs/operation.log`；
- **鉴权**：JWT（`JwtInterceptor`），放行 login / register。

### 8.2 前端规范

- **主题**：中式变量 `--ink/--paper/--line/--ochre/--danger`（见 `theme.css`），不加渐变 / emoji / 骨架滚动动画；
- **组件复用**：PanelCard / StatCard / BarList / TermInput / StatsFilter→RangeFilter / StructuredDataCard（新增），优先复用不新建；
- **请求**：统一走 `utils/request`（响应拦截 + blob 下载 `utils/download`）；接口封装于 `api/*.js`；
- **渐进增强**：mock → 真接口；后端字段缺失时降级隐藏，页面不报错。

### 8.3 契约纪律

- 新增 / 变更接口：**先更新 openapi.yaml，再实现代码**；响应字段以 openapi 为准；
- 错误码查 openapi 附录B（2001 / 4001 / 1006 / 2003 等）；前置校验统一 `IllegalArgumentException` → HTTP 400。

### 8.4 git 工作流

- 每阶段独立 commit、可独立回滚；commit 格式 `feat|fix|docs|refactor(scope): 描述`；
- 前端先行：本阶段前端 + mock 提交审核通过后，方可做本阶段后端；
- 只合 main；过期分支（XXJ / ZRC）不 merge；YC 式移植走手动 cherry-pick 并按新分层重写。

### 8.5 公共资产复用清单

| 层 | 资产 | 用途 |
|----|------|------|
| 后端 | `GlobalExceptionHandler` / `Result` | 统一异常与响应 |
| 后端 | `JwtUtil` / `JwtInterceptor` | 鉴权 |
| 后端 | `DictionaryStore` / `EsTermNormalizer` | 词典缓存 / 三级归一 |
| 后端 | `RecordFilter`（阶段5新增） | 范围 filters 公共过滤 |
| 后端 | `OperationLogger` | 关键操作留痕 |
| 前端 | `PanelCard` / `StatCard` / `BarList` / `TermInput` / `StatsFilter` | 面板 / 指标 / 排行 / 术语输入 / 范围（→RangeFilter） |
| 前端 | `StructuredDataCard`（新增） | structuredData 分区卡片（详情 / 复核复用） |
| 前端 | `utils/request`、`api/*.js` | 请求与接口封装 |

### 8.6 评审职责

| 环节 | 负责人 | 说明 |
|------|--------|------|
| 前端先行 | 功能主导成员 | 提交 mock / 静态实现 |
| 审核 | 组长 / 提报人 | 观感与交互是否达标，未过不得进后端 |
| 后端落地 | 功能主导成员 | 契约以 openapi 为准 |
| 验证 | 主导成员 + 测试成员 | `mvn test` / `npm run build` / 手工场景 |

### 8.7 并发防护约定

- **原则**：单实例部署，不使用分布式锁（Redisson）；采用「幂等 + 轻量任务互斥」；
- **任务互斥**：全库写操作（质控重算 batch / 数据清洗 clean）执行前用 Redis `SETNX tcm:task:{type} NX EX 300`（复用已有 RedisTemplate `setIfAbsent(key, value, Duration)`）抢占，占用中返回"任务进行中"；成功后 `DEL`；
- **幂等**：batch / clean 重复调用结果一致（批量前清理 pending review_tasks、跳过后台 invalid，见 2.5）；
- **词典文件**：导入 / 回滚 / 备份方法加 JVM 锁（synchronized），避免并发覆盖词典 JSON 与 ES 索引重建冲突；
- **扩展预留**：若未来扩展多实例部署，可将 SETNX 互斥升级为 Redisson 分布式锁（注明适用边界后再引依赖）。

### 8.8 角色鉴权约定

| 角色 | 菜单 | 接口权限 |
|------|------|---------|
| 管理员 | 8 项全量（首页/病历/结构化解析/质控校验/复核/清洗导出/词典/审计） | 全部（含治理/词典/质控/导出 等管理操作） |
| 审核员 | 首页看板 + 人工复核 | 登录、看板统计、复核（线C 阶段9） |

- **机制**：`@RequireRole("管理员")` 标注管理接口；`RoleInterceptor` 读 request `currentRole` 校验，未过 → HTTP 403 + Result；前端路由 `meta.roles` + 守卫，`MainLayout` 按登录返回 menus 渲染；
- **演示口径**：菜单可全量展示（截图=管理员视角），但后端强制拦截越权——审核员直输管理 URL 必须 403；
- **新增接口默认**：写操作默认"仅管理员"，只读接口默认"登录即可"，复核专接口默认"双角色"。

---

> 每阶段独立提交、可独立验证；阶段0 为基地基底（修复+权限+登录页），其后阶段1-9 顺延执行。